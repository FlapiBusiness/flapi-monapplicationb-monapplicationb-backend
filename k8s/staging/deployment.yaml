apiVersion: apps/v1
kind: Deployment
metadata:
  name: staging-monapplicationb-monapplicationb-backend-deployment
  namespace: staging-monapplicationb-monapplicationb-backend
  labels:
    app: staging-monapplicationb-monapplicationb-backend
spec:
  selector:
    matchLabels:
      app: staging-monapplicationb-monapplicationb-backend
  template:
    metadata:
      labels:
        app: staging-monapplicationb-monapplicationb-backend
    spec:
      containers:
        - name: staging-monapplicationb-monapplicationb-backend
          image: corentin35/monapplicationb-monapplicationb-backend:staging-${TAG_VERSION}
          imagePullPolicy: Always
          ports:
            - containerPort: 3333
          env:
            - name: PORT
              value: '3333'
          resources:
            requests:
              cpu: '300m'
              memory: '400Mi'
            limits:
              cpu: '600m'
              memory: '800Mi'
      imagePullSecrets:
        - name: regcred
